# Context-Window-Simulator
A Python project that simulates how Large Language Models (LLMs) manage conversation history by counting tokens, enforcing a context window, and removing old messages when the token limit is exceeded.
